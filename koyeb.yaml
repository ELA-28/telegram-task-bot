name: task-bot
type: worker

build:
  type: buildpack
  builder: heroku/buildpacks:20

run:
  command: python bot.py

env:
  - name: BOT_TOKEN
    value: ""

scale:
  min: 1
  max: 1

regions:
  - was

instance_type: nano
